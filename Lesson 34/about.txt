django celery


1. Создать функционал отправки напоминаний на почту о подписанном пользователем событии за день до этого, а также за 6 часов.
2. Кэшировать перечень всех событий на 5 мин.
3. Создать отдельную очередь для email и запустить на ней celery worker.
4. При добавлении нового события в БД создать оповещение всем пользователям, которые подписаны на рассылку.
Подсказка: создать новое поле для модели user — notify (по умолчанию True).

Пример письма (за день до):

Уведомляем вас, что вы согласились посетить «event_name».
«event_description».
Мероприятие проходит завтра в «event_time» «event_place».

Пример письма (новое событие):

Новое мероприятие: «event_name»!
«event_description».
Мероприятие проходит завтра в «event_time» «event_place».


celery -A project worker -Q email --loglevel=info
celery -A project beat --loglevel=info